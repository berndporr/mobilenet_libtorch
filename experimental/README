# Transfer learning with executorch and libtorch

## Install executorch

This installs both the executorch python package and the C++ static libraries

```
git clone https://github.com/pytorch/executorch
cd executorch
source ~/venv/bin/activate
python install_executorch.py
cmake --preset linux -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=/usr/local .
cd cmake-out
make
sudo make install
```

## Compile the transfer learning C++ code

```
cmake .
make
```

## Quantise the Mobilenet V2 model

```
python quantise.py
```

## Do transfer learning

```
./transfer
```

# Further reading

https://android.googlesource.com/platform/external/executorch/+/HEAD/docs/source/extension-module.md

# Credit

Bernd Porr
